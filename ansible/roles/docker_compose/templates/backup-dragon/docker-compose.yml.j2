version: "3.8"
services:
  # Private network
  cloudflared:
    image: cloudflare/cloudflared:2022.6.1
    container_name: cloudflared
    command: tunnel run
    user: root
    restart: always
    volumes:
      - "./cloudflared:/root/.cloudflared"
